import 'package:flutter/material.dart';

class WeightLogScreen extends StatelessWidget {
  const WeightLogScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Weight Log')),
    );
  }
}
